

## Aula de Revisão [12/03]

## > prova/teste AC [17/03]




##### 1 - como identificar uma lista em python?

# é preciso verificar se a variável é do tipo list, para isso 
# podemos usar a função type() ou isinstance(). (fica roxo)

##### 2 - como pegar o 1° elemento de uma lista em python?

# para pegar o 1° elemento de uma lista, basta usar o índice 0,
# pois em python a contagem de índices começa do 0.

##### 3 - como identificar um dicionário em python?

# para identificar um dicionário em python, podemos usar a função type() ou isinstance(),
# verificando se a variável é do tipo dict. (fica roxo) {chave}

##### 4 - como pegar um elemento em dicionário?

# para pegar um elemento em um dicionário, é preciso atribuir um significado à chave,
# com isso, basta referenciar a chave correspondente para acessar o valor associado a ela.
# por exemplo: dicionário[chave]. (fica roxo)

##### 5 - como identfificar uma lista em dicionário?

# para identificar uma lista dentro de um dicionário, podemos acessar a chave
# correspondente e verificar se o valor associado a essa chave é do tipo list, 
# usando a função type() ou isinstance(). (fica roxo)

##### 6 - como transformar uma lista de dicionário em um DataFrame?

# para transformar uma lista de dicionário em um DataFrame, podemos usar
# a função pd.DataFrame() da biblioteca pandas,
# passando a lista de dicionário como argumento. (fica roxo)

##### 7 - como consumir um arquivo csv no DataFrame?

# para consumir um arquivo csv no DataFrame, podemos usar a função pd.read_csv("")
# da biblioteca pandas,
# passando o caminho do arquivo csv como argumento. (fica roxo)

##### 8 - como consumir um arquivo excel no DataFrame?

# para consumir um arquivo excel no DataFrame, podemos usar a função pd.read_excel("")
# da biblioteca pandas,
# passando o caminho do arquivo excel como argumento. (fica roxo)

##### 9 - como filtrar uma coluna de valores?

# para filtrar uma coluna de valores, podemos usar a sintaxe de filtragem do pandas,
# (basicamente > adicionar um filtro = df[df['coluna'] > valor])
# usando a condição desejada para selecionar os valores que atendem a essa condição.

##### 10 - como filtrar uma coluna de string?

# para filtrar uma coluna de string, podemos usar a sintaxe de filtragem do pandas,
# (basicamente > adicionar um filtro = df[df['coluna'] == 'valor'])
# usando a condição desejada para selecionar os valores que atendem a essa condição.

##### 11 - como fazer os filtros no DataFrame?

# para fazer os filtros no DataFrame, podemos usar a sintaxe
# e filtragem do pandas, usando as condições desejadas para selecionar
# os valores que atendem a essas condições.